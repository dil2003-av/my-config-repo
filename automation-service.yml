server:
  port: 8083

spring:
  application:
    name: automation-service

  data:
    mongodb:
      uri: mongodb://localhost:27017/automation_db

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka